/// KHUNG - chưa có logic thật, cần triển khai.
///
/// Bọc quanh package flutter_secure_storage để lưu/đọc/xoá accessToken và
/// refreshToken sau khi đăng nhập (lưu mã hoá, an toàn hơn SharedPreferences).
///
/// Cần có:
/// - saveTokens({accessToken, refreshToken})
/// - readAccessToken() / readRefreshToken()
/// - clear() - gọi lúc đăng xuất.
class SessionStore {
  // TODO: implement
}
